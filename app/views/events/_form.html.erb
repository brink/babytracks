<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <% unless @event.new_record? %>
      <%= f.datetime_select :created_at %><br />
    <% end %>
    <%= f.select :event_type, options_for_select([['Feeding', 'feeding'],['Diaper', 'diaper']], params[:event_type] || @event.event_type) %>
    <br/>
    <%= f.submit :Save %>
  </div>
<% end %>
