<h1>Listing events</h1>

<table>

<% @events.each do |event| %>
  <tr>
    <td><%= link_to event.event_type.humanize, edit_event_path(event)%></td>
    <td><%= event.created_at.to_formatted_s(:long) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event', new_event_path %>
